# Используйте базовый образ Node.js
FROM node:14

# Установите рабочую директорию внутри контейнера
WORKDIR /app

# Копируйте зависимости и исходный код сервера
COPY package*.json ./
RUN npm install
COPY . .

# Указываем порт, который будет использоваться сервером
EXPOSE 3001

# Команда для запуска сервера
CMD ["node", "server.js"]
